spring:
  boot:
    admin:
      client:
        enabled: true
        username: admin
        password: admin
  security:
    user:
      name: admin
      password: admin
management:
  endpoints:
    web:
      exposure:
        include:
        - "*"
  endpoint:
    health:
      show-details: always